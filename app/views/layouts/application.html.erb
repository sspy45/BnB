<!DOCTYPE html>
<html>
  <head>
    <%= csrf_meta_tags %>

    <%= stylesheet_link_tag    'application', media: 'all' %>
    <%= javascript_include_tag 'application' %>

    <script type="text/javascript">
      <% if signed_in? %>
        window.currentUser = <%= render(
          "api/partials/user.json.jbuilder",
          user: current_user
        ).html_safe %>
        console.log(currentUser);
      <% end %>
    </script>

  </head>

  <body>
    <div id='root'></div>
  </body>
</html>
